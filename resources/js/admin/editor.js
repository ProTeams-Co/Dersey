/**
 * Structural stub — CKEditor 5 wiring lands in Batch 3.0 alongside the real
 * content-editing admin screens. Exists now so admin.js's module wiring
 * doesn't have to change shape later.
 */
function init() {
    //
}

export default { init };
